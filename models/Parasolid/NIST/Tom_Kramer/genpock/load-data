; LOAD PROCESS PLAN
(load "../../parts/genpock/plan.olps")

; LOAD BLOCK BREP
(put-data 'workpieces 'block3
  (fetch (step-to-plist '../../parts/block-6.5-3.0-1.5/brep-design.step-comp
			'(block))
	 'data))

; LOAD DESIGN BREP
(put-data 'designs 'genpock-design
  (fetch (step-to-plist '../../parts/block-6.5-3.0-1.5/brep-design.step-comp
                        '(design))
	 'data))

; LOAD MRSEVS
(put-data 'mrsevs 'genpock-mrsevs
  (fetch (step-to-plist '../../parts/genpock/mrsevs.step  '(mrsevs)) 'data))

; LOAD SETUP
(step-to-plist '../../parts/genpock/setup.step 'step-in)
(link-data (fetch 'step-in 'data))
(put-data 'setups 'genpock-setup (fetch 'step-in 'data))

(setf (symbol-plist 'step-in) nil)

; Brep data are not linked during loading because they must be transformed
; before linking.  The linking is done during initialization of writing
; a program.
