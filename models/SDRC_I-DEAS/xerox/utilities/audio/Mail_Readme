Edit the audio_script file in the xerox/utilities/audio directory and change the 
path of the soundfile high_score.aiff to your installation path.


To attach a soundfile to a mail message :

SGI:
Start the mailbox manually with the following command:

mailbox -m "zmail -gui" -B ../success/xerox/utilities/audio/audio_script

where../success points to the location of the installed demo audio_script file

Configure the project prior to ws2 entering the project.
